<Window x:Class="WpfApp_Bing源与路径.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_Bing源与路径"
        mc:Ignorable="d"
        Title="MainWindow" Height="150" Width="500">
    <StackPanel>
        <!--<TextBox x:Name="textBox1" Text="{Binding Path=Value,ElementName=slider1}" BorderBrush="Black" Margin="5"/>
        <Slider x:Name="slider1" Maximum="100" Minimum="5"/>-->
        <TextBox x:Name="textBox1" BorderBrush="Black" Margin="5"/>
        <!--<TextBox x:Name="textBox2" Text="{Binding Path=Text.Length,ElementName= textBox1,Mode=OneWay}" BorderBrush="Black" Margin="5"/>-->
        <TextBox x:Name="textBox2" Text="{Binding Path=Text[],ElementName= textBox1,Mode=OneWay}" BorderBrush="Black" Margin="5"/>
    </StackPanel>
</Window>
